version: '3.8'

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - REDIS_URL=${REDIS_URL}
      - PORT=3001
    volumes:
      - ./uploads:/app/uploads
